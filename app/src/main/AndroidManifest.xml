<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aguillen.supermarketshoppingmobile">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.aguillen.supermarketshoppingmobile.activity.ArticleCreateActivity"></activity>
        <activity android:name="com.aguillen.supermarketshoppingmobile.activity.RowArticleActivity" />
        <activity android:name="com.aguillen.supermarketshoppingmobile.activity.ArticlesListActivity" />
        <activity android:name="com.aguillen.supermarketshoppingmobile.activity.MenuActivity" />
        <activity android:name="com.aguillen.supermarketshoppingmobile.activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>